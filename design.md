# Design — KAWANAI

このアプリの唯一のデザイン基準。以降の画面追加・改修はこのファイルを読んでから実装する。
ページごとに新しい配色やフォントを作らない。システムを育てたいときはこのファイルを直す。

対象ユーザーは「若い女性」だけではない。10 代から高齢層まで、性別も問わず使う。
したがって「コスメらしい可愛さ」ではなく、**成分と色を数値で確かめる道具**としての中立な佇まいを基準にする。
彩度の高い色は商品スウォッチ（`ProductThumb` / `.swatch` / 口コミ画像）だけが持つ。UI 側は退く。

## Genre

modern-minimal（editorial 寄り）。装飾ではなく、罫線・余白・数字の並びで階層をつくる。

## Theme

アンカーは深いティール（hue 200 前後）。中性色はアンカーに寄せて僅かに寒色へ振る。
ピンク／マゼンタ／パープルは UI から外す（グラデーションも含めて全廃）。

| token | oklch | hex | 用途 |
| --- | --- | --- | --- |
| `--color-paper` | `oklch(98% 0.004 230)` | `#f6f9fb` | body 背景 |
| `ink-0` | `oklch(99.7% 0.002 230)` | `#fdfeff` | カード面 |
| `ink-50` | `oklch(97.2% 0.005 230)` | `#f2f6f8` | 面の一段沈み |
| `ink-100` | `oklch(94.8% 0.006 230)` | `#eaeff1` | 塗りの薄い区画 |
| `ink-200` | `oklch(89.8% 0.008 230)` | `#d8dee2` | 罫線・境界 |
| `ink-400` | `oklch(62% 0.011 230)` | `#80888c` | 補助テキスト |
| `ink-500` | `oklch(52% 0.012 230)` | `#626a6f` | 補助テキスト（強） |
| `ink-600` | `oklch(44% 0.013 230)` | `#4c5458` | 本文（副） |
| `ink-700` | `oklch(34% 0.013 230)` | `#31393d` | 本文（強） |
| `ink-900` | `oklch(21% 0.012 235)` | `#13191d` | 見出し・主要 CTA 地 |
| `brand-50` | `oklch(97% 0.015 200)` | `#eaf8f9` | アクセントの最薄面 |
| `brand-100` | `oklch(94% 0.028 200)` | `#d7f1f2` | バッジ地 |
| `brand-200` | `oklch(88% 0.045 200)` | `#b6e1e3` | 境界（アクセント） |
| `brand-300` | `oklch(78% 0.065 200)` | `#85c4c8` | 図版・淡い塗り |
| `brand-400` | `oklch(65% 0.085 202)` | `#459fa5` | ホバー面・リング |
| `brand-500` | `oklch(56% 0.09 204)` | `#18838d` | アイコン |
| `brand-600` | `oklch(48% 0.088 206)` | `#006b76` | リンク・アクティブ・主アクセント |
| `brand-700` | `oklch(40% 0.078 208)` | `#00535d` | ホバー時の文字色 |
| `brand-800` | `oklch(33% 0.065 210)` | `#003e48` | 濃い面 |
| `brand-900` | `oklch(27% 0.05 212)` | `#002d35` | 最濃 |
| `clay-100` | `oklch(94% 0.03 55)` | `#fce6d9` | 注意・被り検出の地 |
| `clay-300` | `oklch(80% 0.07 50)` | `#e4b196` | 注意の境界 |
| `clay-500` | `oklch(62% 0.11 48)` | `#bb7048` | 注意アイコン |
| `clay-700` | `oklch(45% 0.10 45)` | `#824121` | 注意テキスト |

- アクセント（`brand-*` の 500〜700）は 1 画面の面積で **5% 以下**。大きな面をアクセントで塗らない。
- `clay-*` は第 2 アクセントで、意味は「注意 / 被り / 割高」のみ。装飾に使わない。既存の `plum-*` はこれに置き換えて廃止する。
- 純黒 `#000` / 純白 `#fff` を新規に足さない。既存の `bg-white` は面トークン `ink-0` に寄せる。
- グラデーション背景は使わない（`brand-gradient` は削除、`brand-soft` は単色面に置換）。
- コントラストは本文 4.5:1 以上。`brand-600` は `ink-0` 上で本文可、白文字を載せる地としても可。

## Typography

日本語のウェブフォントは重いので、**欧文だけ webfont、和文はシステム**という現状の方針を維持する。

- Display: システム明朝（`"Hiragino Mincho ProN", "Yu Mincho", "Noto Serif JP", Georgia, serif`）。
  Playfair Display は落とす（結婚式招待状の気配が強く、対象を広げる意図と合わない）。見出しは和文が主役なので明朝で組む。
- Body: Zen Kaku Gothic New（latin subset）+ システムゴシック。現状維持。
- Outlier: IBM Plex Mono 400/600（latin subset）。ワードマーク「KAWANAI」、数値（点数・価格・件数）、
  セクションの通し番号やラベルにのみ使う。文章には使わない。
- 見出しは常にローマン体。斜体見出しは禁止。強調はウェイトとアクセント色で取る。
- 数値は `tabular-nums` + mono。価格・点数が縦に揃うこと。

## Spacing / Shape

- 4pt スケール。Tailwind 既定のスペーシングをそのまま使う。
- 角丸は絞る: カード `rounded-xl`(12px) / 入力 `rounded-lg` / ピル型は CTA と絞り込みチップだけ。
  `rounded-3xl` `rounded-4xl` の大きな丸みは使わない（柔らかすぎ、対象年齢が狭く見える）。
- 影は原則使わず、`border border-ink-200` の 1px 罫線で面を切る。`shadow-card` は
  浮かせる必要がある要素（ドロワー・モーダル・スティッキー）に限る。

## Motion

- 既存の `rise` を残す。イージングは `ease-out`、200〜260ms。
- スクロール連動リビール、パララックスは入れない。
- `prefers-reduced-motion: reduce` では opacity のみ・150ms 以下。

## CTA voice

- Primary: `bg-ink-900 text-ink-0`、`rounded-full`、太字、アイコンは右。
- Secondary: `border border-ink-200 bg-ink-0`、同じ形。
- テキストリンク: `text-brand-600`、ホバーで `text-brand-700` + 下線。
- 「買わない」を勧める行動（削除・見送り）は `clay-700` のテキストリンクで、赤の警告色は使わない。

## 画面ごとの約束

- 共有するもの: ワードマーク、アクセント色とその使用量、フォント、CTA の形、見出しの組み方
  （mono の小ラベル → 明朝の見出し → ゴシックの説明文）。
- 変えていいもの: セクションの並びと粗密。ランディングのみ図版的な表現を許す。
- アプリ側画面（`/search` `/stash` `/scan` `/color` `/feed` `/me` `/settings`）には装飾を足さない。
  機能が画面をもたせる。

## 避けるもの（この UI で実際に出ていた AI っぽさ）

1. ピンク→パープルのグラデーション見出し / ヒーロー。
2. 角丸正方形にアイコンを入れた 3 枚並びの機能カード（アイコンは見出しにインラインで置くか外す）。
3. 全部が同じ padding・同じ角丸・同じ影で並ぶリズムのない画面。
4. 中央寄せの連続。ヒーローは左寄せ、幅は非対称に。
5. 未定義トークン（`ink-0` `ink-500` `ink-700` は使われているのに定義がなかった）。定義してから使う。
